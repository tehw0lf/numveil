name: weekly dast scan

on:
  schedule:
    - cron: "0 3 * * 1"
  workflow_dispatch:

jobs:
  dast:
    name: external workflow
    uses: tehw0lf/workflows/.github/workflows/security-scan-dast.yml@main
    permissions:
      contents: read
      issues: write
      security-events: write
    with:
      target_url: "https://numveil.tehwolf.de"
      scan_type: "full"
      max_duration_minutes: "45"
      rules_file: ".zap/rules.tsv"
